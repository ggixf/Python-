#Peça ao usuário para inserir a base e a altura de um triângulo e calcule a área.

base = float(input("Digite a base: "))
altura = float(input("Digite sua altura: "))
area = base * altura
print(f"A base é {area}.")
